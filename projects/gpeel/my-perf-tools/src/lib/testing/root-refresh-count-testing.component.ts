import {Component, DoCheck} from '@angular/core';


@Component({
  selector: 'root-refresh-count',
  template: ``,
})
export class RootRefreshCountTestingComponent implements DoCheck {
  ngDoCheck(): void {}
}
